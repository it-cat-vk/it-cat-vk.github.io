<?php
$n = sizeof(load("news"));
echo("
		<h1>Новости (".$n.")</h1>
");

$beg = $n - 1 - $num * $pg;
$end = $n - $num * ($pg + 1);

for($i = $beg; $i >= 0 && $i >= $end; --$i)
{
	$fname = "news/n".($i + 1).".dat";
	if(!file_exists($fname))
	{
		continue;
	}
	include($fname);
	echo("
<section>");
	if($nd != false)
	{
		echo("
	<h2>Тут кто-то нагадил :(</h2>");
	}
	else
	{
//		$ectext = substr($ntext, 0, 200);
//		if(sizeof($ntext) > 200)
//			$ectext .= " <...>";
		$ectext = $ntext;
		echo("
	<h2><a href=\"/?page=new&post=".($i + 1)."\">".$ns."</a></h2>
".$ectext."
	<div>Выложил ".$na." в <time datetime=\"".date("c", $nt)."\">".date("r", $nt)."</time></div>
");
	}
	echo("
</section>");
}

echo("
		<nav>На страницу:
");
for($i = 0; $i < $n / $num; ++$i)
{
	echo("
			<a class=\"pagenav\" href=\"/?page=news&num=".$num."&pg=".$i."\">".($i + 1)."</a>");
}
echo("
		</nav>
");
?>
